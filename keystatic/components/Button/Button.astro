---
const { label, href, newWindow = true } = Astro.props
---

<p>
  <a
    href={href}
    target={newWindow ? '_blank' : '_self'}
    rel={newWindow ? 'noopener noreferrer' : undefined}
    class="my-0 inline-block rounded-lg bg-blue-600 px-4 py-2 text-sm font-medium text-white no-underline transition-colors hover:bg-blue-700"
  >
    {label}
  </a>
</p>
