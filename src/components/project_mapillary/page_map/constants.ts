/** Basemap layer id: insert custom layers *below* this so city names stay on top. Must exist in `BaseMap` style (OpenFreeMap Positron uses OMT-style ids). */
export const BEFORE_CITY_LABELS = 'label_city'
